# MCP Integration

- Configure MCP clients using the JSON files in `agents/tools/mcp`.
- Each config lists allowed paths and permissions.
- Requests touching protected directories require human approval.
