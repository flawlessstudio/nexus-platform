# Brand Onboarding

1. Duplicate `/brands/_template` (TBD) and rename to the new brand slug.
2. Fill `brand.json` and `tokens.json` with validated data.
3. Populate foundations, identity, editorial, templates, and assets directories.
4. Run `ops/scripts/validate-all.sh`.
5. Notify agent ops to index the new brand.
